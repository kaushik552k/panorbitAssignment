import React from "react";

const Helper = ({ text }) => {
  return (
    <div className="inline-block m-auto font-extrabold tracking-widest text-8xl opacity-30 mt-20">
      <h1>{text}</h1>
    </div>
  );
};

export default Helper;
